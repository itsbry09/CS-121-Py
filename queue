class Queue:
    def __init__(self):
        self.items = []

    def enqueue(self, item):
        self.items.append(item)

    def dequeue(self):
        if self.items:
            return self.items.pop(0)
        return None

queue = Queue()
queue.enqueue("a")
queue.enqueue("b")
queue.enqueue("c")
print("Queue front:", queue.items)
print("Dequeue:", queue.dequeue())
